# Gold SFT baseline at batch_size=1 (same data budget as SDFT).

model:
  name: LiquidAI/LFM2.5-230M
  dtype: float32

data:
  dataset: yahma/alpaca-cleaned
  split: train
  prompt_fields: [instruction, input]
  response_field: output
  num_examples: 32
  seed: 0

generation:
  out_path: data/compare/batch1_gold.jsonl

lora:
  r: 8
  alpha: 16
  dropout: 0.05
  target_modules: .*self_attn\.(q|k|v|out)_proj

training:
  output_dir: outputs/compare/batch1-sft-gold
  epochs: 1
  lr: 0.0001
  batch_size: 1
  grad_accum: 1
  max_length: 512
  warmup_steps: 0
  logging_steps: 4
  save_strategy: "no"
  seed: 0
  target_field: response
